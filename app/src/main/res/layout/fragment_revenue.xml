<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    tools:context=".revenue.RevenueFragment">

   <androidx.appcompat.widget.SearchView
       android:id="@+id/frg_revenue_search_bar"
       app:layout_constraintTop_toTopOf="parent"
       android:layout_width="match_parent"
       android:layout_height="30dp"
       android:layout_marginLeft="@dimen/margin_side"
       android:layout_marginStart="@dimen/margin_side"
       android:layout_marginRight="@dimen/margin_side"
       android:layout_marginEnd="@dimen/margin_side"
       android:layout_marginTop="@dimen/margin_side"
       android:background="@drawable/custom_brackground"/>

   <androidx.recyclerview.widget.RecyclerView
       app:layoutManager="androidx.recyclerview.widget.LinearLayoutManager"
       android:id="@+id/frg_revenue_rcv"
       app:layout_constraintTop_toBottomOf="@id/frg_revenue_search_bar"
       app:layout_constraintBottom_toBottomOf="parent"
       android:layout_width="match_parent"
       android:layout_marginTop="@dimen/margin_side"
       android:layout_marginBottom="@dimen/margin_side"

       android:layout_height="0dp"/>

</androidx.constraintlayout.widget.ConstraintLayout>